# Leave empty or omit in dev - uses Vite proxy so API requests appear under
# the frontend origin in Network inspector (POST /api/login, GET /api/me, etc.)
# Set only if proxy fails: VITE_API_URL=http://localhost:3001
